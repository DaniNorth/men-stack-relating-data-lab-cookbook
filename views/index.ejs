<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
</head>
<body>
    <%- include('partials/_navbar.ejs') %>

    <h1>Welcome to the Cooked App</h1>

    <% if(user) { %>
        <p>Welcome, <%= user.username %>!</p>
        <a href="/users/<%= user._id %>/recipes">View Your Recipes</a>
    <% } else { %>
        <p>Please <a href="/auth/sign-in">sign in</a> or <a href="/auth/sign-up">sign up</a>.</p>
    <% } %>
</body>
</html>